# Base URL of the backend API, including the /api suffix.
# Local dev can leave this unset (defaults to http://127.0.0.1:8000/api).
# In production (Vercel) set this to your deployed backend, e.g.
VITE_API_BASE_URL=https://your-backend.onrender.com/api
